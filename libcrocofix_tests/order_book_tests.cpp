#include <catch.hpp>
#include <libcrocofix/order_book.hpp>

using namespace crocofix;

TEST_CASE("OrderBook", "[order_book]") {

    SECTION("Timestamp format seconds") 
    {
        // REQUIRE(crocofix::timestamp_string(crocofix::timestamp_format::seconds).size() == 17);
    }
}